<!doctype html>
{# Landing page for the published site. Rendered by make_gallery.py. #}
<meta charset="utf-8">
<title>sliding-squares</title>
<style>
 :root{--bg:#fff;--fg:#1a1a1a;--muted:#666;--line:#ddd;--card:#fafafa}
 @media (prefers-color-scheme:dark){:root{--bg:#24262b;--fg:#f2f2f2;--muted:#a9adb5;
       --line:#3d414a;--card:#2d3038}}
 body{margin:0;padding:40px 24px;background:var(--bg);color:var(--fg);
      font:15px/1.6 ui-sans-serif,system-ui,sans-serif;max-width:640px}
 h1{font-size:22px;margin:0 0 6px}
 a.card{display:block;background:var(--card);border:1px solid var(--line);
        border-radius:8px;padding:14px 16px;margin:12px 0;color:inherit;
        text-decoration:none}
 a.card:hover{border-color:var(--muted)}
 a.card b{display:block;font-size:16px;margin-bottom:2px}
 .muted{color:var(--muted)}
</style>

<h1>sliding-squares</h1>
<p class="muted">Generated reports. Rebuilt on every push — never committed.</p>

<a class="card" href="tests/index.html">
  <b>Test runs</b>
  <span class="muted">Every test case: the solved sequence stepped one control
  switch at a time, the blocker heatmap, and each simplification recipe with a
  side-by-side comparison.</span>
</a>

<a class="card" href="gallery/index.html">
  <b>Recipe gallery</b>
  <span class="muted">What each simplification recipe does to corner, edge,
  junction and degenerate layouts — the worked cases behind the test suite.</span>
</a>

{# Only linked when the benchmark has actually been built: benchmark_bfs.py is
   a separate run, and a card pointing at a missing page is worse than none. #}
{% if has_benchmark %}
<a class="card" href="benchmark/index.html">
  <b>BFS comparison</b>
  <span class="muted">The three searches — mirror, bidirectional and
  unidirectional — measured against each other on every test case for states
  held, solve time and peak memory, with the switch counts cross-checked.</span>
</a>
{% endif %}

<p class="muted">Built {{ generated }}</p>
