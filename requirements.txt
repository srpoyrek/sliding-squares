matplotlib>=3.5.0
pre-commit>=3.5.0
ruff>=0.4.4
numpy>=2.0.0

# Optional: code knowledge graph tooling (scripts/graphify/).
# Skip this line if you only want to run the solver and tests.
graphifyy>=0.4.23