repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff-format    # auto-formats code
      - id: ruff           # catches lint errors
        args: [--fix]